[pytest]
testpaths = tests
python_files = test_*.py
# Avoid writing .pyc under tests/ during collection (see tests/conftest.py).
addopts = -q
markers =
    integration: subprocess / loopback UDP (run: pytest -m integration)
