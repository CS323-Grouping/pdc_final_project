class_name PalettePreset extends Resource

## Per-environment colour palette. Currently just bg + accent — expand when
## the env theming gets richer (Phase 4b).

@export var background_color: Color = Color(0.07, 0.09, 0.14, 1.0)
@export var accent_color: Color = Color(0.7, 0.85, 1.0, 1.0)
