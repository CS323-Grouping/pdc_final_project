extends Control

## Settings screen — placeholder.
##
## Phase 8 delivers the actual resolution / fullscreen / control scheme / perf
## overlay controls. Until then this scene just hosts the placeholder body and
## the reusable BackButton.
##
## (Nothing to wire — BackButton handles itself; ESC is global via SceneManager.)
