extends Control

## Avatar editor — placeholder.
##
## Phase 6 delivers the actual editor (model picker, color choices, head image
## upload + crop, save → server). Until then this scene just hosts placeholder
## body content and the reusable BackButton.
##
## (Nothing to wire — BackButton handles itself; ESC is global via SceneManager.)
